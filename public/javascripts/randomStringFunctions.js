function firstReadWR1() {
  var result;
  var output;
  var count = 0;
  var count2 = 0;
  for(var key in QuarterbackReads.FirstReadStrings.WR1)
    if (Math.random() < 1/++count) {
       result = key;
    }
    for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
      if(Math.random() < 1/++count2) {
        output = QuarterbackReads.FirstReadStrings.WR1[result][value];
      }
    console.log(output);
  }

function firstReadWR2() {
  var result;
  var output;
  var count = 0;
  var count2 = 0;
  for(var key in QuarterbackReads.FirstReadStrings.WR2)
    if (Math.random() < 1/++count) {
       result = key;
    }
    for(var value in QuarterbackReads.FirstReadStrings.WR2[result]);
      if(Math.random() < 1/++count2) {
        output = QuarterbackReads.FirstReadStrings.WR2[result][value];
      }
    console.log(output);
  }

  function firstReadWR3() {
    var result;
    var output;
    var count = 0;
    var count2 = 0;
    for(var key in QuarterbackReads.FirstReadStrings.WR3)
      if (Math.random() < 1/++count) {
         result = key;
      }
      for(var value in QuarterbackReads.FirstReadStrings.WR3[result]);
        if(Math.random() < 1/++count2) {
          output = QuarterbackReads.FirstReadStrings.WR3[result][value];
        }
      console.log(output);
    }

    function secondReadWR1() {
      var result;
      var output;
      var count = 0;
      var count2 = 0;
      for(var key in QuarterbackReads.FirstReadStrings.WR1)
        if (Math.random() < 1/++count) {
           result = key;
        }
        for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
          if(Math.random() < 1/++count2) {
            output = QuarterbackReads.FirstReadStrings.WR1[result][value];
          }
        console.log(output);
      }

    function secondReadWR2() {
      var result;
      var output;
      var count = 0;
      var count2 = 0;
      for(var key in QuarterbackReads.FirstReadStrings.WR2)
        if (Math.random() < 1/++count) {
           result = key;
        }
        for(var value in QuarterbackReads.FirstReadStrings.WR2[result]);
          if(Math.random() < 1/++count2) {
            output = QuarterbackReads.FirstReadStrings.WR2[result][value];
          }
        console.log(output);
      }

    function secondReadWR3() {
      var result;
      var output;
      var count = 0;
      var count2 = 0;
      for(var key in QuarterbackReads.FirstReadStrings.WR3)
        if (Math.random() < 1/++count) {
           result = key;
        }
        for(var value in QuarterbackReads.FirstReadStrings.WR3[result]);
          if(Math.random() < 1/++count2) {
            output = QuarterbackReads.FirstReadStrings.WR3[result][value];
          }
        console.log(output);
      }

    function thirdReadWR1() {
      var result;
      var output;
      var count = 0;
      var count2 = 0;
      for(var key in QuarterbackReads.FirstReadStrings.WR1)
        if (Math.random() < 1/++count) {
           result = key;
        }
        for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
          if(Math.random() < 1/++count2) {
            output = QuarterbackReads.FirstReadStrings.WR1[result][value];
          }
        console.log(output);
      }

      function thirdReadWR2() {
        var result;
        var output;
        var count = 0;
        var count2 = 0;
        for(var key in QuarterbackReads.FirstReadStrings.WR2)
          if (Math.random() < 1/++count) {
             result = key;
          }
          for(var value in QuarterbackReads.FirstReadStrings.WR2[result]);
            if(Math.random() < 1/++count2) {
              output = QuarterbackReads.FirstReadStrings.WR2[result][value];
            }
          console.log(output);
        }

      function thirdReadWR3() {
        var result;
        var output;
        var count = 0;
        var count2 = 0;
        for(var key in QuarterbackReads.FirstReadStrings.WR3)
          if (Math.random() < 1/++count) {
             result = key;
          }
          for(var value in QuarterbackReads.FirstReadStrings.WR3[result]);
            if(Math.random() < 1/++count2) {
              output = QuarterbackReads.FirstReadStrings.WR3[result][value];
            }
          console.log(output);
        }

      function fourthReadWR1() {
        var result;
        var output;
        var count = 0;
        var count2 = 0;
        for(var key in QuarterbackReads.FirstReadStrings.WR1)
          if (Math.random() < 1/++count) {
             result = key;
          }
          for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
            if(Math.random() < 1/++count2) {
              output = QuarterbackReads.FirstReadStrings.WR1[result][value];
            }
          console.log(output);
        }

        function fourthReadWR2() {
          var result;
          var output;
          var count = 0;
          var count2 = 0;
          for(var key in QuarterbackReads.FirstReadStrings.WR2)
            if (Math.random() < 1/++count) {
               result = key;
            }
            for(var value in QuarterbackReads.FirstReadStrings.WR2[result]);
              if(Math.random() < 1/++count2) {
                output = QuarterbackReads.FirstReadStrings.WR2[result][value];
              }
            console.log(output);
          }

          function fourthReadWR3() {
            var result;
            var output;
            var count = 0;
            var count2 = 0;
            for(var key in QuarterbackReads.FirstReadStrings.WR3)
              if (Math.random() < 1/++count) {
                 result = key;
              }
              for(var value in QuarterbackReads.FirstReadStrings.WR3[result]);
                if(Math.random() < 1/++count2) {
                  output = QuarterbackReads.FirstReadStrings.WR3[result][value];
                }
              console.log(output);
            }

          function fifthReadWR1() {
            var result;
            var output;
            var count = 0;
            var count2 = 0;
            for(var key in QuarterbackReads.FirstReadStrings.WR1)
              if (Math.random() < 1/++count) {
                 result = key;
              }
              for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
                if(Math.random() < 1/++count2) {
                  output = QuarterbackReads.FirstReadStrings.WR1[result][value];
                }
              console.log(output);
            }

            function fifthReadWR2() {
              var result;
              var output;
              var count = 0;
              var count2 = 0;
              for(var key in QuarterbackReads.FirstReadStrings.WR2)
                if (Math.random() < 1/++count) {
                   result = key;
                }
                for(var value in QuarterbackReads.FirstReadStrings.WR2[result]);
                  if(Math.random() < 1/++count2) {
                    output = QuarterbackReads.FirstReadStrings.WR2[result][value];
                  }
                console.log(output);
              }

              function fifthReadWR3() {
                var result;
                var output;
                var count = 0;
                var count2 = 0;
                for(var key in QuarterbackReads.FirstReadStrings.WR1)
                  if (Math.random() < 1/++count) {
                     result = key;
                  }
                  for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
                    if(Math.random() < 1/++count2) {
                      output = QuarterbackReads.FirstReadStrings.WR1[result][value];
                    }
                  console.log(output);
                }

              function sixthReadWR1() {
                var result;
                var output;
                var count = 0;
                var count2 = 0;
                for(var key in QuarterbackReads.FirstReadStrings.WR1)
                  if (Math.random() < 1/++count) {
                     result = key;
                  }
                  for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
                    if(Math.random() < 1/++count2) {
                      output = QuarterbackReads.FirstReadStrings.WR1[result][value];
                    }
                  console.log(output);
                }

            function sixthReadWR2() {
              var result;
              var output;
              var count = 0;
              var count2 = 0;
              for(var key in QuarterbackReads.FirstReadStrings.WR2)
                if (Math.random() < 1/++count) {
                   result = key;
                }
                for(var value in QuarterbackReads.FirstReadStrings.WR2[result]);
                  if(Math.random() < 1/++count2) {
                    output = QuarterbackReads.FirstReadStrings.WR2[result][value];
                  }
                console.log(output);
              }

            function sixthReadWR3() {
              var result;
              var output;
              var count = 0;
              var count2 = 0;
              for(var key in QuarterbackReads.FirstReadStrings.WR3)
                if (Math.random() < 1/++count) {
                   result = key;
                }
                for(var value in QuarterbackReads.FirstReadStrings.WR3[result]);
                  if(Math.random() < 1/++count2) {
                    output = QuarterbackReads.FirstReadStrings.WR3[result][value];
                  }
                console.log(output);
              }

              function seventhReadWR1() {
                var result;
                var output;
                var count = 0;
                var count2 = 0;
                for(var key in QuarterbackReads.FirstReadStrings.WR1)
                  if (Math.random() < 1/++count) {
                     result = key;
                  }
                  for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
                    if(Math.random() < 1/++count2) {
                      output = QuarterbackReads.FirstReadStrings.WR1[result][value];
                    }
                  console.log(output);
                }

            function seventhReadWR2() {
              var result;
              var output;
              var count = 0;
              var count2 = 0;
              for(var key in QuarterbackReads.FirstReadStrings.WR2)
                if (Math.random() < 1/++count) {
                   result = key;
                }
                for(var value in QuarterbackReads.FirstReadStrings.WR2[result]);
                  if(Math.random() < 1/++count2) {
                    output = QuarterbackReads.FirstReadStrings.WR2[result][value];
                  }
                console.log(output);
              }

          function seventhReadWR3() {
            var result;
            var output;
            var count = 0;
            var count2 = 0;
            for(var key in QuarterbackReads.FirstReadStrings.WR3)
              if (Math.random() < 1/++count) {
                 result = key;
              }
              for(var value in QuarterbackReads.FirstReadStrings.WR3[result]);
                if(Math.random() < 1/++count2) {
                  output = QuarterbackReads.FirstReadStrings.WR3[result][value];
                }
              console.log(output);
            }

          function eigthReadWR1() {
            var result;
            var output;
            var count = 0;
            var count2 = 0;
            for(var key in QuarterbackReads.FirstReadStrings.WR1)
              if (Math.random() < 1/++count) {
                 result = key;
              }
              for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
                if(Math.random() < 1/++count2) {
                  output = QuarterbackReads.FirstReadStrings.WR1[result][value];
                }
              console.log(output);
            }

            function eigthReadWR2() {
              var result;
              var output;
              var count = 0;
              var count2 = 0;
              for(var key in QuarterbackReads.FirstReadStrings.WR2)
                if (Math.random() < 1/++count) {
                   result = key;
                }
                for(var value in QuarterbackReads.FirstReadStrings.WR2[result]);
                  if(Math.random() < 1/++count2) {
                    output = QuarterbackReads.FirstReadStrings.WR2[result][value];
                  }
                console.log(output);
              }


          function eigthReadWR3() {
            var result;
            var output;
            var count = 0;
            var count2 = 0;
            for(var key in QuarterbackReads.FirstReadStrings.WR3)
              if (Math.random() < 1/++count) {
                 result = key;
              }
              for(var value in QuarterbackReads.FirstReadStrings.WR3[result]);
                if(Math.random() < 1/++count2) {
                  output = QuarterbackReads.FirstReadStrings.WR3[result][value];
                }
              console.log(output);
            }


          function ninthReadWR1() {
            var result;
            var output;
            var count = 0;
            var count2 = 0;
            for(var key in QuarterbackReads.FirstReadStrings.WR1)
              if (Math.random() < 1/++count) {
                 result = key;
              }
              for(var value in QuarterbackReads.FirstReadStrings.WR1[result]);
                if(Math.random() < 1/++count2) {
                  output = QuarterbackReads.FirstReadStrings.WR1[result][value];
                }
              console.log(output);
            }

          function ninthReadWR2() {
            var result;
            var output;
            var count = 0;
            var count2 = 0;
            for(var key in QuarterbackReads.FirstReadStrings.WR2)
              if (Math.random() < 1/++count) {
                 result = key;
              }
              for(var value in QuarterbackReads.FirstReadStrings.WR2[result]);
                if(Math.random() < 1/++count2) {
                  output = QuarterbackReads.FirstReadStrings.WR2[result][value];
                }
              console.log(output);
            }

          function ninthReadWR3() {
            var result;
            var output;
            var count = 0;
            var count2 = 0;
            for(var key in QuarterbackReads.FirstReadStrings.WR3)
              if (Math.random() < 1/++count) {
                 result = key;
              }
              for(var value in QuarterbackReads.FirstReadStrings.WR3[result]);
                if(Math.random() < 1/++count2) {
                  output = QuarterbackReads.FirstReadStrings.WR3[result][value];
                }
              console.log(output);
            }